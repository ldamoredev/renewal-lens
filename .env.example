# Required from Phase 2 onward for live screenshot analysis.
ANTHROPIC_API_KEY=

# Keep the model configurable. Phase 2 will select and document the exact Haiku default.
ANTHROPIC_MODEL=

# Railway injects PORT at runtime. Do not hard-code it here.
